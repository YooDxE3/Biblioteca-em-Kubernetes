apiVersion: v1
kind: Service
metadata:
  name: biblioteca
  namespace: biblioteca-t3
spec:
  type: NodePort
  selector:
    app: biblioteca
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
